import 'package:flutter/material.dart';
class InlayPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Inlay'),
        elevation: 0,
      ),
      body: Container(
        child: Center(child: Text('Inlay')),
      ),
    );
  }
}
